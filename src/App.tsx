import { Button } from "antd";
import React from "react";

const App: React.FC = () => {
  return (
    <div>
      <h1>Hello, React + TypeScript + Sass!</h1>
      <Button type="primary">这是一个antd的按钮</Button>
    </div>
  );
};

export default App;
